extends basic_layout

block content
    h2 Admin, please, introduce yourself
    form(action="./testPOST" method="POST")
        label(for="login") Login:
        input(
            type="text"
            id="login"
            name="login"
        )
        br
        br
        label(for="password") Password:
        input(
            type="password"
            id="password"
            name="password"
        )
        br
        br
        input(type="submit" value="Let me in")